索引的删除算法还没实现

tmpindex合并到ehash的判断标准过于简单:当tmpindex达到90%满就merge
实现效果很差，应该需要改成另的方式，比如说tmpindex内存使用量达到多少...done

单个索引项大小超过一块的情况还没实现....done

权重的数据是用double型表示的，并且完全没压缩的
空间浪费是很严重的．应该改为float，并且找种合适的压缩算法...done

目前是假设directory表能够完全放在内存
如果假设不成立，就要添加directory表写到磁盘的算法

cache模块使用的是lru算法
改用nru算法，用双链hash表实现，可以提高性能

bucket模块使用一些结构体的技巧，结构体对齐会浪费一些空间
也许用宏实现可以提升一点点性能


